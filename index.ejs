<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Create User</title>

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            darkbg: '#0f172a',   // background
            cardbg: '#020617'    // card
          }
        }
      }
    }
  </script>
</head>
<body class="min-h-screen flex items-center justify-center bg-darkbg">
    
  <form method="POST" action="/create" class="bg-cardbg p-8 rounded-xl shadow-2xl w-80">
    <h2 class="text-white text-2xl font-semibold text-center mb-6"> Create User
    </h2>
     <input
      type="text"
      name="name"
      placeholder="name"
      class="w-full mb-4 px-4 py-2 rounded-md bg-slate-800 text-white placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-green-500"
    />

    <input
      type="email"
      name="email"
      placeholder="Email"
      required
      class="w-full mb-6 px-4 py-2 rounded-md bg-slate-800 text-white placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-green-500"
    />
    
    <input
      type="text"
      name="image" placeholder="image url" class="w-full mb-4 px-4 py-2 rounded-md bg-slate-800 text-white placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-green-500"/>
    <input type="submit" class="w-full py-2 rounded-md bg-green-600 hover:bg-green-700 text-white font-semibold transition" value="Create User"
      >
    
   <a href="/read" class="text-blue-500 mt-10">Read</a>
  </form>

</body>
</html>
